pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Building App from GIT'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying App from git'
            }
        }
    }
}
